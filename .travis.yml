language: node_js
node_js:
    - 8

branches:
  only:
    - /chore\(package\) v\d+\.\d+\.\d+/

script:
  - npm run build

notifications:
    email:
        on_success: never
        on_failure: change

sudo: false

